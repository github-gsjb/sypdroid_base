package com.android.apparatus.menu;import android.content.Context;import android.view.View;import android.view.ViewGroup;import android.widget.BaseAdapter;import android.widget.TextView;import net.majorkernelpanic.spydroid.R;public class SubordinateMenuAdapter extends BaseAdapter {    private String[] listMore;    private Context mContext;    // 默认选中第一个    private int selectPosition = 0;    private Hoder hoder;    public SubordinateMenuAdapter(Context context, String[] list) {        this.listMore = list;        this.mContext = context;    }    public void setSelectItem(int position) {        this.selectPosition = position;    }    public int getSelectItem() {        return this.selectPosition;    }    @Override    public int getCount() {        return listMore.length;    }    @Override    public Object getItem(int position) {        return listMore[position];    }    @Override    public long getItemId(int position) {        return position;    }    @Override    public View getView(int position, View convertView, ViewGroup parent) {        if (convertView == null) {            convertView = View.inflate(mContext, R.layout.activity_menu_lv_subordinatelist_item, null);            hoder = new Hoder(convertView);            convertView.setTag(hoder);        } else {            hoder = (Hoder) convertView.getTag();        }        hoder.textView.setText(listMore[position]);        if (position == selectPosition) {            hoder.textView.setTextColor(0xFFFF8C00);        }        return convertView;    }    private static class Hoder {        private TextView textView;        public Hoder(View view) {            textView = (TextView) view.findViewById(R.id.moreItem_text);        }    }}